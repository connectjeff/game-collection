# TODO

- Add a web UI action to download public barcode source CSVs and rebuild local barcode caches without using the CLI.
- Track barcode source provenance per row in the review UI so manual reviewers can see where a suggested title came from.
- Add incremental cache refresh support for Wikidata and CSV URL sources.
- Replace legacy `crop_path` field names with upload/sample image names once existing review CSV compatibility is no longer needed.
